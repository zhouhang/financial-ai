## 1. 后端流式推送实现

- [x] 1.1 修改 LLM 调用逻辑，支持回调函数逐句推送响应
- [x] 1.2 在 WebSocket 消息处理中添加 stream 类型消息的发送逻辑
- [x] 1.3 添加任务取消机制，支持用户中断流式输出

## 2. 前端流式接收实现

- [x] 2.1 确保 App.tsx 中 stream 类型消息处理逻辑正确工作
- [x] 2.2 验证 stream 和 message 消息的衔接逻辑（去重）
- [x] 2.3 在 handleSendMessage 中添加清除 streamingMessageId 的逻辑，支持中断
- [x] 2.4 在 MessageBubble 组件中添加闪烁光标样式，流式输出时显示，完成后隐藏

## 3. 测试与验证

- [x] 3.1 测试流式输出功能：发送消息后观察响应是否逐步显示
- [x] 3.2 测试中断功能：在流式输出过程中发送新消息，验证是否正确中断
- [x] 3.3 测试消息完整性：确认最终显示的完整内容与后端发送的一致
- [x] 3.4 验证刷新页面、切换会话等场景下流式输出的正确性
