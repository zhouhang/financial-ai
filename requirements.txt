# Playwright MCP Server 依赖

# 核心依赖
playwright>=1.40.0
mcp>=0.9.0
starlette>=0.27.0
uvicorn[standard]>=0.24.0
anyio>=4.0.0

# 数据验证和类型
pydantic>=2.0.0

# 测试框架（可选）
pytest>=7.0.0
pytest-playwright>=0.4.0

# HTTP 客户端（可选，用于测试）
requests>=2.31.0
