# Playwright MCP Server 依赖

# 核心依赖
playwright>=1.40.0
mcp>=0.9.0
starlette>=0.27.0
uvicorn[standard]>=0.24.0
anyio>=4.0.0

# 数据验证和类型
pydantic>=2.0.0

# 测试框架（可选）
pytest>=7.0.0
pytest-playwright>=0.4.0

# HTTP 客户端
requests>=2.31.0
httpx>=0.25.0

# 数据处理（用于 Reconciliation MCP Server）
pandas>=2.0.0
openpyxl>=3.1.0  # Excel 文件支持
chardet>=5.0.0  # 编码检测，防止中文乱码

# 安全表达式评估
simpleeval>=0.9.13  # 安全替代 eval()，防止代码注入
