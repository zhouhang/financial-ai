---
phase: 01-wizard-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - finance-web/src/components/ReconWorkspace.tsx
  - finance-web/src/components/recon/SchemeWizardSummaryStep.tsx
autonomous: true
requirements:
  - FLOW-01
user_setup: []
must_haves:
  truths:
    - Step 4 is a read-only summary and save gate, not a second editing workspace.
    - The save button stays disabled until the current `proc` and `recon` trial states are both `passed`.
  artifacts:
    - finance-web/src/components/recon/SchemeWizardSummaryStep.tsx renders the Step 4 summary cards and gate state chips.
    - finance-web/src/components/ReconWorkspace.tsx compiles the unchanged `/recon/schemes` payload from the step-sliced draft state and Step 4 summary.
  key_links:
    - Step 4 summary cards read from the authoritative `intent`, `preparation`, `reconciliation`, and `derived` slices.
    - `handleCreateScheme` and footer button disabled state use the same derived trial pass conditions shown in the summary UI.
---

<objective>
完成第四步确认页和保存门禁壳，让新的 4 步向导在 Phase 1 就形成完整闭环。

Purpose: 即使后续第二步、第三步能力还会继续增强，财务用户也要能在第四步看到清楚的摘要、状态和保存条件，而不是回到一个技术化的保存收口。
Output: `SchemeWizardSummaryStep.tsx`、更新后的第四步渲染逻辑，以及与状态切片一致的保存门禁表达。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wizard-foundation/01-CONTEXT.md
@.planning/phases/01-wizard-foundation/01-UI-SPEC.md
@.planning/phases/01-wizard-foundation/01-VALIDATION.md
@.planning/phases/01-wizard-foundation/01-01-PLAN.md
@.planning/phases/01-wizard-foundation/01-02-PLAN.md
@finance-web/src/components/ReconWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a dedicated Step 4 summary component with business-facing status cards</name>
  <files>finance-web/src/components/recon/SchemeWizardSummaryStep.tsx</files>
  <read_first>AGENTS.md, .planning/phases/01-wizard-foundation/01-CONTEXT.md, .planning/phases/01-wizard-foundation/01-UI-SPEC.md, finance-web/src/components/ReconWorkspace.tsx, finance-web/src/components/recon/SchemeWizardReconStep.tsx</read_first>
  <action>Create `finance-web/src/components/recon/SchemeWizardSummaryStep.tsx` as a read-only summary surface. It must render summary groups titled `方案目标`, `数据准备`, `对账规则`, and `校验状态`. Each group should show concise business-facing values from the wizard slices, plus visible state chips using the exact labels `待生成`, `待试跑`, `仅供参考`, and `已通过` where applicable. The component must not render editable `textarea`, `input`, or `select` controls for the main scheme configuration. Keep the content focused on what will be saved and which trial status is still blocking save.</action>
  <verify>rg -n "方案目标|数据准备|对账规则|校验状态|待生成|待试跑|仅供参考|已通过" finance-web/src/components/recon/SchemeWizardSummaryStep.tsx</verify>
  <acceptance_criteria>
    - `finance-web/src/components/recon/SchemeWizardSummaryStep.tsx` contains `方案目标`, `数据准备`, `对账规则`, and `校验状态`
    - `finance-web/src/components/recon/SchemeWizardSummaryStep.tsx` contains `待生成`, `待试跑`, `仅供参考`, and `已通过`
    - `finance-web/src/components/recon/SchemeWizardSummaryStep.tsx` does not contain `<textarea`
  </acceptance_criteria>
  <done>Step 4 exists as a dedicated summary screen that explains current readiness in finance language without creating a second editing surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Step 4 summary data and save gating into ReconWorkspace</name>
  <files>finance-web/src/components/ReconWorkspace.tsx</files>
  <read_first>AGENTS.md, .planning/phases/01-wizard-foundation/01-CONTEXT.md, .planning/phases/01-wizard-foundation/01-VALIDATION.md, finance-web/src/components/ReconWorkspace.tsx, finance-web/src/components/recon/SchemeWizardSummaryStep.tsx, finance-web/src/components/recon/schemeWizardState.ts</read_first>
  <action>Update `finance-web/src/components/ReconWorkspace.tsx` so step 4 renders `SchemeWizardSummaryStep` instead of the current minimal summary card block. Compute the Step 4 props from the authoritative wizard slices and the derived pass/stale state that came from `schemeWizardState.ts`. Keep the existing `/recon/schemes` save payload keys exactly as `scheme_name`, `description`, `scheme_meta_json`, `dataset_bindings_json`, `proc_rule_json`, and `recon_rule_json`, but build them from the shared state adapter instead of ad-hoc flat-object reads. Keep the save button disabled unless both current trial states are `passed`, and show an inline footer or step-body explanation that saving requires both Step 2 and Step 3 trials to pass. Do not add Step 4 edit controls in this plan.</action>
  <verify>rg -n "SchemeWizardSummaryStep|scheme_name|dataset_bindings_json|proc_rule_json|recon_rule_json|保存方案" finance-web/src/components/ReconWorkspace.tsx</verify>
  <acceptance_criteria>
    - `finance-web/src/components/ReconWorkspace.tsx` renders `SchemeWizardSummaryStep`
    - `finance-web/src/components/ReconWorkspace.tsx` still contains `scheme_name`, `dataset_bindings_json`, `proc_rule_json`, and `recon_rule_json`
    - `finance-web/src/components/ReconWorkspace.tsx` disables the save path unless both trial states are `passed`
    - `finance-web/src/components/ReconWorkspace.tsx` contains an inline save-gate explanation instead of relying only on hidden stale state
  </acceptance_criteria>
  <done>Step 4 becomes the visible save gate for the new wizard shell while preserving the current backend contract and final validation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Step 4 summary view vs. the authoritative wizard state that will actually be saved
- Save footer gating vs. backend `/recon/schemes` final validation
- Business summary labels vs. technical payload compilation

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Save gate state | mitigate | Derive the Step 4 gate and the footer button disabled state from the same current `proc` / `recon` trial state so the UI cannot claim readiness while blocking or vice versa. |
| T-01-08 | Repudiation | Summary vs. saved payload | mitigate | Compile the final `/recon/schemes` payload from the same shared wizard-state adapter used to render Step 4, reducing mismatch between what the user sees and what is saved. |
| T-01-09 | Information Disclosure | Step 4 surface | accept | The plan keeps technical JSON details out of the main Step 4 body; advanced JSON remains a secondary capability outside this summary-first phase. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd finance-web && npm run lint`
- [ ] `cd finance-web && npm run build`
- [ ] Step 4 shows summary cards and gate messaging without editable business controls
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Step 4 clearly communicates what is ready to save and what still blocks save
- The save gate is consistent across button state, summary copy, and payload compilation
  </success_criteria>

<output>
After completion, create `.planning/phases/01-wizard-foundation/01-03-SUMMARY.md`
</output>
