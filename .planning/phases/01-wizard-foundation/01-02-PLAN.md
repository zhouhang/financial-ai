---
phase: 01-wizard-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - finance-web/src/components/ReconWorkspace.tsx
  - finance-web/src/components/recon/SchemeWizardIntentStep.tsx
  - finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx
autonomous: true
requirements:
  - FLOW-01
user_setup: []
must_haves:
  truths:
    - The wizard visibly uses four finance-facing steps named `方案目标`, `数据准备`, `对账规则`, and `确认保存`.
    - Step 1 contains only the business-intent fields `方案名称` and `对账目的`, with no dataset selector or JSON-first controls.
  artifacts:
    - finance-web/src/components/recon/SchemeWizardIntentStep.tsx renders the dedicated Step 1 business-intent form.
    - finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx becomes a Step 2 `数据准备` shell instead of a mixed Step 1 / Step 2 screen.
  key_links:
    - ReconWorkspace routes step 1 to `SchemeWizardIntentStep` and step 2 to the narrowed `SchemeWizardTargetProcStep`.
    - Step navigation and inline warning placement match the UI contract instead of relying on only the modal-top error banner.
---

<objective>
把四步向导的外壳和第一步交互改成真正面向财务用户的业务入口。

Purpose: 让用户一进入向导就先说明“这次要做什么对账”，把数据集选择和整理动作明确放到第二步，建立正确的操作心智。
Output: 新的 `SchemeWizardIntentStep.tsx`、更新后的步骤标题和只承担“数据准备”职责的 Step 2 组件。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wizard-foundation/01-CONTEXT.md
@.planning/phases/01-wizard-foundation/01-UI-SPEC.md
@.planning/phases/01-wizard-foundation/01-RESEARCH.md
@.planning/phases/01-wizard-foundation/01-01-PLAN.md
@finance-web/src/components/ReconWorkspace.tsx
@finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated Step 1 business-intent component</name>
  <files>finance-web/src/components/recon/SchemeWizardIntentStep.tsx</files>
  <read_first>AGENTS.md, .planning/phases/01-wizard-foundation/01-CONTEXT.md, .planning/phases/01-wizard-foundation/01-UI-SPEC.md, finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx, finance-web/src/index.css</read_first>
  <action>Create `finance-web/src/components/recon/SchemeWizardIntentStep.tsx` as the sole Step 1 surface. The component must render only two editable fields labeled `方案名称` and `对账目的`, with finance-facing helper copy from the UI contract. Include the empty-state heading `先补充方案目标，再进入数据准备` and the body copy `填写方案名称和对账目的后，系统再带你配置左右数据与后续试跑。` Do not include dataset selectors, left/right descriptions, AI generation buttons, JSON textareas, or any `proc` / `recon` technical heading in this component.</action>
  <verify>rg -n "方案名称|对账目的|先补充方案目标，再进入数据准备|填写方案名称和对账目的后，系统再带你配置左右数据与后续试跑。" finance-web/src/components/recon/SchemeWizardIntentStep.tsx</verify>
  <acceptance_criteria>
    - `finance-web/src/components/recon/SchemeWizardIntentStep.tsx` contains `方案名称`
    - `finance-web/src/components/recon/SchemeWizardIntentStep.tsx` contains `对账目的`
    - `finance-web/src/components/recon/SchemeWizardIntentStep.tsx` contains `先补充方案目标，再进入数据准备`
    - `finance-web/src/components/recon/SchemeWizardIntentStep.tsx` does not contain `选择左侧数据集` or `选择右侧数据集`
  </acceptance_criteria>
  <done>Step 1 is isolated into a small business-intent form that fits within one viewport and does not leak later-step configuration controls.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the wizard shell and Step 2 component around the new step boundaries</name>
  <files>finance-web/src/components/ReconWorkspace.tsx, finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx</files>
  <read_first>AGENTS.md, .planning/phases/01-wizard-foundation/01-CONTEXT.md, .planning/phases/01-wizard-foundation/01-UI-SPEC.md, finance-web/src/components/ReconWorkspace.tsx, finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx, finance-web/src/components/recon/SchemeWizardIntentStep.tsx</read_first>
  <action>Update `finance-web/src/components/ReconWorkspace.tsx` so `SCHEME_WIZARD_STEPS` uses the exact step titles `方案目标`, `数据准备`, `对账规则`, and `确认保存`. Route step 1 to `SchemeWizardIntentStep`, step 2 to `SchemeWizardTargetProcStep`, step 3 to the existing recon step, and step 4 to the summary shell added in the next plan. Rewrite `SchemeWizardTargetProcStep.tsx` so it no longer accepts a mixed `step: 1 | 2` branch. Its visible heading and section copy must present Step 2 as `数据准备`, including left/right dataset selection, the placeholder block title `筛选数据 / 行数据操作（后续支持）`, and inline stale/reference warnings positioned near the Step 2 action area instead of only at modal top. Keep JSON only behind the advanced-view action; do not promote JSON as the primary editing surface.</action>
  <verify>rg -n "方案目标|数据准备|对账规则|确认保存|筛选数据 / 行数据操作（后续支持）|SchemeWizardIntentStep" finance-web/src/components/ReconWorkspace.tsx finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx</verify>
  <acceptance_criteria>
    - `finance-web/src/components/ReconWorkspace.tsx` contains the exact step titles `方案目标`, `数据准备`, `对账规则`, and `确认保存`
    - `finance-web/src/components/ReconWorkspace.tsx` renders `SchemeWizardIntentStep`
    - `finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx` contains `筛选数据 / 行数据操作（后续支持）`
    - `finance-web/src/components/recon/SchemeWizardTargetProcStep.tsx` no longer declares `export type SchemeWizardStep = 1 | 2`
  </acceptance_criteria>
  <done>The wizard shell now teaches the correct 4-step workflow, and Step 2 is clearly framed as data preparation rather than a mixed technical screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Step navigation state vs. what the user believes each step is responsible for
- Business-facing copy vs. legacy technical terminology still present in existing components
- Inline action-area warnings vs. modal-top-only error banners

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Wizard step boundaries | mitigate | Use exact step titles and dedicated step components so Step 1 cannot impersonate a complete data-preparation screen. |
| T-01-05 | Tampering | Step 2 stale-state communication | mitigate | Move stale/reference warnings into the Step 2 action area so users can see when generation or trial results are no longer current before clicking the next action. |
| T-01-06 | Information Disclosure | Business-facing copy | mitigate | Remove default `proc` / `recon` wording from visible step titles and Step 1 content; keep technical wording only in advanced JSON view or low-level error detail. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd finance-web && npm run test:components`
- [ ] Step 1 renders only business-intent controls
- [ ] Step badges and step titles match the UI contract exactly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The create-scheme modal now opens on a concise Step 1 instead of mixing dataset configuration into the first screen
- Step 2 visibly reserves space for later data-preparation capabilities without implementing them yet
  </success_criteria>

<output>
After completion, create `.planning/phases/01-wizard-foundation/01-02-SUMMARY.md`
</output>
