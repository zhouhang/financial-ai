---
phase: 06-multi-sheet-upload-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - finance-agents/data-agent/utils/file_intake.py
  - finance-agents/data-agent/graphs/main_graph/public_nodes.py
  - finance-agents/data-agent/graphs/proc/nodes.py
  - finance-agents/data-agent/graphs/recon/execution_service.py
autonomous: true
requirements:
  - FILE-01
  - FILE-04
user_setup: []
must_haves:
  truths:
    - 文件型 `proc` / `recon` 需要一份共享的“逻辑上传文件列表”，而不是各自重新解释多 sheet 工作簿。
    - 逻辑上传文件既要有真实 `file_path`，也要有原工作簿和 sheet 溯源信息。
  artifacts:
    - `finance-agents/data-agent/utils/file_intake.py` 定义逻辑上传文件展开、命名和映射构建的共享工具。
    - `public_nodes.py` 会把逻辑上传文件元数据写入共享 ctx，供后续 plan 接管正式 file_check。
    - `proc` 与 `recon` 的文件路径映射函数支持优先使用 ctx 中的逻辑上传文件列表。
  key_links:
    - 同一份逻辑上传文件元数据必须能同时驱动 `proc_task_execute_node` 和 `build_recon_inputs_from_file_matches`。
---

<objective>
为多 sheet 工作簿建立共享的逻辑文件基础设施和贯穿式路径映射。

Purpose: 先把“工作簿 -> 逻辑文件 -> 统一 name/path map”这条基础链路建出来，避免后续 prefilter 与正式 file_check 接入时还要同时处理路径契约问题。
Output: 一个新的共享 intake 模块，以及能在 ctx 中携带逻辑上传文件元数据的 `check_file_node` / `proc` / `recon` 基础接线。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md
@.planning/phases/06-multi-sheet-upload-intake/06-RESEARCH.md
@.planning/phases/06-multi-sheet-upload-intake/06-VALIDATION.md
@finance-agents/data-agent/graphs/main_graph/public_nodes.py
@finance-agents/data-agent/graphs/proc/nodes.py
@finance-agents/data-agent/graphs/recon/execution_service.py
@finance-mcp/security_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared logical-upload intake module</name>
  <files>finance-agents/data-agent/utils/file_intake.py</files>
  <read_first>AGENTS.md, .planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md, .planning/phases/06-multi-sheet-upload-intake/06-RESEARCH.md, finance-agents/data-agent/graphs/main_graph/public_nodes.py, finance-mcp/tools/file_upload_tool.py, finance-mcp/security_utils.py</read_first>
  <action>Create `finance-agents/data-agent/utils/file_intake.py` and define one shared contract for logical upload files. The module must accept the existing raw `uploaded_files` shapes (`str` path or `{file_path, original_filename}` dict), normalize them, and expose helper(s) that: `1)` pass through CSV and single-sheet files unchanged, `2)` expand multi-sheet workbooks into sheet-level logical file entries with real `file_path`, `display_name`, `original_filename`, `sheet_name`, and `sheet_index`, and `3)` build display-name / stored-name to `file_path` maps from either raw or logical entries. The split files must live under the existing upload-root contract so downstream readers can continue consuming `/uploads/...` references without DSL changes.</action>
  <verify>rg -n "sheet_name|sheet_index|display_name|build_upload_name_maps|logical upload" finance-agents/data-agent/utils/file_intake.py</verify>
  <acceptance_criteria>
    - `finance-agents/data-agent/utils/file_intake.py` exists and supports raw uploaded files plus multi-sheet workbook expansion
    - the module emits logical file metadata containing `file_path`, `display_name`, `original_filename`, `sheet_name`, and `sheet_index`
    - the module exposes a shared name-map builder instead of leaving duplicate map logic in `proc` and `recon`
  </acceptance_criteria>
  <done>The repo has one reusable source of truth for logical upload file metadata, naming, and path maps.</done>
</task>

<task type="auto">
  <name>Task 2: Thread logical-upload metadata through shared file-check context and downstream execution mappers</name>
  <files>finance-agents/data-agent/graphs/main_graph/public_nodes.py, finance-agents/data-agent/graphs/proc/nodes.py, finance-agents/data-agent/graphs/recon/execution_service.py</files>
  <read_first>AGENTS.md, .planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md, finance-agents/data-agent/utils/file_intake.py, finance-agents/data-agent/graphs/main_graph/public_nodes.py, finance-agents/data-agent/graphs/proc/nodes.py, finance-agents/data-agent/graphs/recon/execution_service.py</read_first>
  <action>Update the shared file-check path so `check_file_node()` can build and persist `logical_uploaded_files` (or an equivalent explicit ctx field) without yet changing the formal matcher behavior in this plan. Then update the downstream file-path resolution helpers in `proc/nodes.py` and `recon/execution_service.py` so they prefer the ctx logical file list when present and fall back to `state.uploaded_files` for backward compatibility. Do not change the `proc_execute` or `recon_execute` request schema in this plan; the change here is shared mapping readiness and compatibility-safe plumbing.</action>
  <verify>rg -n "logical_uploaded_files|build_upload_name_maps" finance-agents/data-agent/graphs/main_graph/public_nodes.py finance-agents/data-agent/graphs/proc/nodes.py finance-agents/data-agent/graphs/recon/execution_service.py</verify>
  <acceptance_criteria>
    - `check_file_node()` writes a dedicated logical-upload field into shared ctx
    - `proc` execution path can build file maps from the logical-upload field when present
    - `recon` execution path can build file maps from the logical-upload field when present
    - existing raw-upload flows still have a fallback path and do not require DSL or API contract changes
  </acceptance_criteria>
  <done>The codebase can now carry logical upload files through the shared context without breaking existing single-file behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Raw uploaded files vs. generated logical sheet files
- Shared ctx metadata vs. downstream `proc` / `recon` file-path resolution
- User-visible display names vs. server-side stored file refs

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | logical upload file map | mitigate | Use one shared map builder and one ctx field so `proc` / `recon` cannot drift onto different file-name interpretations. |
| T-06-02 | Repudiation | split-file traceability | mitigate | Persist `original_filename`, `sheet_name`, and `sheet_index` on every logical file so later errors can be traced back to the source workbook. |
| T-06-03 | Information Disclosure | upload storage contract | accept | Split files continue using the existing `/uploads/...` contract and are not exposed via any new public API surface. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/kevin/workspace/financial-ai && source .venv/bin/activate && pytest finance-agents/data-agent/tests/test_file_intake.py -q`
- [ ] Single-sheet and CSV uploads still fall back cleanly when no logical-upload field is present
- [ ] `proc` and `recon` downstream mapping helpers can resolve logical file display names to real `file_path`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has one shared logical-upload contract instead of duplicate file-name map helpers
- Downstream execution code is ready to consume split-sheet logical files in the next plan
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-sheet-upload-intake/06-01-SUMMARY.md`
</output>
