---
phase: 06-multi-sheet-upload-intake
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - finance-agents/data-agent/utils/file_intake.py
  - finance-agents/data-agent/graphs/main_graph/public_nodes.py
  - finance-mcp/tools/file_validate_tool.py
autonomous: true
requirements:
  - FILE-02
  - FILE-03
  - FILE-04
user_setup: []
must_haves:
  truths:
    - sheet 级预筛选只负责剔除明显无效的 sheet，不能替代正式 schema 匹配
    - 宽松 schema 的歧义必须继续显示候选映射，而不是被预筛选吞掉
  artifacts:
    - `file_intake.py` 能输出预筛选结果、保留/过滤原因和稳定命名
    - `public_nodes.py` 使用预筛选后的逻辑文件列表调用正式 `validate_files`
    - 失败消息能显示 `candidate_mappings` 和预筛选摘要
  key_links:
    - 正式 file_check 统计的“上传文件数量”必须基于预筛选后的逻辑文件，而不是原始 workbook 数或全部 sheet 数
---

<objective>
把多 sheet 工作簿真正切入正式 file_check：增加 sheet 级预筛选、稳定命名和歧义提示增强。

Purpose: 让说明页、空白页、明显不命中 schema 的 sheet 不再干扰正式 file_check，同时保留现有 `validate_files` 对真实歧义的权威判断。
Output: 启用 prefilter 后的 `check_file_node()`、稳定命名的逻辑文件，以及包含候选映射的可解释失败消息。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md
@.planning/phases/06-multi-sheet-upload-intake/06-RESEARCH.md
@.planning/phases/06-multi-sheet-upload-intake/06-VALIDATION.md
@.planning/phases/06-multi-sheet-upload-intake/06-01-PLAN.md
@finance-agents/data-agent/utils/file_intake.py
@finance-agents/data-agent/graphs/main_graph/public_nodes.py
@finance-mcp/tools/file_validate_tool.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement conservative sheet-level prefiltering and stable logical-file naming</name>
  <files>finance-agents/data-agent/utils/file_intake.py</files>
  <read_first>AGENTS.md, .planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md, .planning/phases/06-multi-sheet-upload-intake/06-RESEARCH.md, finance-agents/data-agent/utils/file_intake.py, finance-mcp/tools/file_validate_tool.py</read_first>
  <action>Extend `finance-agents/data-agent/utils/file_intake.py` so each sheet candidate is evaluated before formal validation. The prefilter must only drop sheets when one of these conditions is true: `1)` header row is empty, `2)` there are no data rows, or `3)` after applying the same column-normalization / alias logic used by formal validation, the sheet cannot satisfy any schema's `required_columns`. The helper must keep a per-sheet summary with `kept/dropped` status and reason codes. Final logical display names must remain human-readable and end with a legal extension, while also being unique and stable across multiple workbooks in one upload batch.</action>
  <verify>rg -n "prefilter|dropped_reason|sheet_index|display_name" finance-agents/data-agent/utils/file_intake.py</verify>
  <acceptance_criteria>
    - prefilter has explicit dropped reasons for empty header, no data row, and no schema candidate
    - stable logical file names include workbook identity plus sheet traceability while preserving a valid extension
    - prefilter does not choose a single schema when multiple candidates still exist
  </acceptance_criteria>
  <done>The intake module can now safely decide which sheet files are eligible for formal validation without collapsing real ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Switch formal file_check to prefiltered logical files and surface ambiguity hints</name>
  <files>finance-agents/data-agent/graphs/main_graph/public_nodes.py, finance-mcp/tools/file_validate_tool.py</files>
  <read_first>AGENTS.md, .planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md, finance-agents/data-agent/utils/file_intake.py, finance-agents/data-agent/graphs/main_graph/public_nodes.py, finance-mcp/tools/file_validate_tool.py</read_first>
  <action>Update `check_file_node()` so it calls the intake helper, sends only `kept` logical files into `validate_files`, and keeps the prefilter summary in ctx for later messaging. When `validate_files` fails because of ambiguous mappings, ensure the user-facing error message expands `candidate_mappings` into readable lines that reference the logical display name and candidate `table_name` list. If small adjustments are needed in `finance-mcp/tools/file_validate_tool.py`, limit them to preserving or clarifying `candidate_mappings` and do not change the DSL contract or matching semantics.</action>
  <verify>rg -n "candidate_mappings|prefilter|logical_uploaded_files|本次共上传|可匹配" finance-agents/data-agent/graphs/main_graph/public_nodes.py finance-mcp/tools/file_validate_tool.py</verify>
  <acceptance_criteria>
    - `check_file_node()` counts only prefiltered logical files when invoking formal validation
    - ambiguous mappings are visible in the returned failure message instead of only generic wording
    - prefilter summary is retained in ctx or logs for later troubleshooting
    - `validate_files` remains the authoritative schema matcher
  </acceptance_criteria>
  <done>Formal file_check now works on the right logical inputs and produces user-actionable ambiguity feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Prefilter eligibility logic vs. authoritative formal schema matcher
- User-visible ambiguity hints vs. internal `candidate_mappings` structure
- Logical display names vs. actual stored upload refs

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | prefilter logic | mitigate | Restrict prefilter to obvious-invalid cases only and reuse the same normalization / alias standard as formal validation. |
| T-06-05 | Spoofing | logical file names | mitigate | Require stable names with workbook identity and sheet index so two different logical files cannot impersonate each other in downstream maps. |
| T-06-06 | Repudiation | ambiguity feedback | mitigate | Expand `candidate_mappings` into readable user messages and keep prefilter summaries so failures are explainable after the fact. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/kevin/workspace/financial-ai && source .venv/bin/activate && pytest finance-agents/data-agent/tests/test_file_intake.py finance-mcp/tests/test_file_validate_tool.py -q`
- [ ] A workbook with extra instruction / blank sheets no longer fails only because the old count gate sees every sheet
- [ ] A workbook with ambiguous business sheets still returns candidate schema hints instead of silently choosing one
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Formal validation receives only the intended logical file candidates
- Real schema ambiguity remains visible and explainable to the user
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-sheet-upload-intake/06-02-SUMMARY.md`
</output>
