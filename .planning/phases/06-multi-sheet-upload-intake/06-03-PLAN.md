---
phase: 06-multi-sheet-upload-intake
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - finance-agents/data-agent/tests/test_file_intake.py
  - finance-agents/data-agent/tests/recon/test_execution_service.py
  - finance-mcp/tests/test_file_validate_tool.py
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03
  - FILE-04
user_setup: []
must_haves:
  truths:
    - 多 sheet 改造需要同时守住 split/prefilter、正式 file_check，以及 `proc` / `recon` 执行映射三条链路
    - 回归必须覆盖说明页、空白页、宽松 schema 歧义和逻辑文件路径映射，而不是只测一个 happy path
  artifacts:
    - `test_file_intake.py` 覆盖逻辑文件展开、命名、预筛选和摘要
    - `test_execution_service.py` 覆盖 `recon` 对逻辑文件映射的消费
    - `test_file_validate_tool.py` 或等价测试覆盖歧义候选和最终正式验证行为
  key_links:
    - Phase 6 的测试必须能证明“不改 DSL 也能吃多 sheet workbook”这个目标成立
---

<objective>
补齐多 sheet intake 的目标回归覆盖，锁住 split/prefilter/mapping 的关键风险点。

Purpose: 这次改造的风险不在一处，而在多个边界一起联动；没有针对性回归的话，后续非常容易在旧数量门槛、命名冲突或执行映射上复发。
Output: 数据代理侧和 MCP 侧的一组 targeted pytest，用来守住多 sheet 文件 intake 的关键行为。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md
@.planning/phases/06-multi-sheet-upload-intake/06-RESEARCH.md
@.planning/phases/06-multi-sheet-upload-intake/06-VALIDATION.md
@.planning/phases/06-multi-sheet-upload-intake/06-01-PLAN.md
@.planning/phases/06-multi-sheet-upload-intake/06-02-PLAN.md
@finance-agents/data-agent/utils/file_intake.py
@finance-agents/data-agent/graphs/main_graph/public_nodes.py
@finance-agents/data-agent/graphs/recon/execution_service.py
@finance-mcp/tools/file_validate_tool.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused tests for sheet split, prefilter, and stable naming</name>
  <files>finance-agents/data-agent/tests/test_file_intake.py</files>
  <read_first>AGENTS.md, .planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md, .planning/phases/06-multi-sheet-upload-intake/06-VALIDATION.md, finance-agents/data-agent/utils/file_intake.py</read_first>
  <action>Create `finance-agents/data-agent/tests/test_file_intake.py` and add targeted tests for: `1)` single-sheet and CSV pass-through behavior, `2)` multi-sheet workbook expansion into logical files, `3)` prefilter dropping empty-header / no-data / no-schema-candidate sheets, and `4)` stable display-name generation without collisions across multiple workbooks in one batch. Use small synthetic workbooks created inside the tests rather than relying on checked-in fixtures.</action>
  <verify>rg -n "prefilter|sheet|display_name|no_schema_candidate|no_data" finance-agents/data-agent/tests/test_file_intake.py</verify>
  <acceptance_criteria>
    - the new test file covers pass-through and multi-sheet split paths
    - there is at least one test for each prefilter drop reason
    - naming collision prevention is asserted in tests
  </acceptance_criteria>
  <done>The intake helper is protected against the main logic regressions that Phase 6 introduces.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-path regressions for ambiguity surfacing and recon input mapping</name>
  <files>finance-agents/data-agent/tests/recon/test_execution_service.py, finance-mcp/tests/test_file_validate_tool.py</files>
  <read_first>AGENTS.md, .planning/phases/06-multi-sheet-upload-intake/06-CONTEXT.md, .planning/phases/06-multi-sheet-upload-intake/06-VALIDATION.md, finance-agents/data-agent/graphs/recon/execution_service.py, finance-mcp/tools/file_validate_tool.py</read_first>
  <action>Extend or create targeted tests so the repo explicitly verifies: `1)` `build_recon_inputs_from_file_matches()` can resolve logical file names back to the split `file_path`, and `2)` formal validation still returns candidate mappings when a kept logical file can match multiple schemas. Cover at least one ambiguity case caused by宽松 `required_columns`, and one case where extra instruction sheets are filtered before they can inflate file-count checks.</action>
  <verify>rg -n "candidate_mappings|logical file|build_recon_inputs_from_file_matches|ambigu" finance-agents/data-agent/tests/recon/test_execution_service.py finance-mcp/tests/test_file_validate_tool.py</verify>
  <acceptance_criteria>
    - recon-side tests prove logical file names can be resolved to split-sheet file paths
    - MCP-side tests prove ambiguity is still surfaced after prefiltering
    - at least one test covers “extra instruction sheet should not count as a required file”
  </acceptance_criteria>
  <done>The end-to-end risk areas of multi-sheet intake are now guarded by reproducible tests across both data-agent and MCP layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Test-generated synthetic workbooks vs. runtime upload contract
- data-agent file-intake behavior vs. MCP validator semantics
- recon input mapping vs. final `file_path` consumption

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation | regression coverage | mitigate | Encode every critical failure mode as a reproducible pytest instead of relying on one manual workbook. |
| T-06-08 | Tampering | recon logical-file mapping | mitigate | Add explicit tests that `build_recon_inputs_from_file_matches()` resolves split-sheet names to the right file path. |
| T-06-09 | Denial of Service | noisy extra sheets | mitigate | Add tests proving instruction / blank sheets are filtered before they can trip formal file-count limits. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/kevin/workspace/financial-ai && source .venv/bin/activate && pytest finance-agents/data-agent/tests/test_file_intake.py finance-agents/data-agent/tests/recon/test_execution_service.py finance-mcp/tests/test_file_validate_tool.py -q`
- [ ] The suite covers split, prefilter, ambiguity, and recon/proc path-mapping concerns
- [ ] No new tests depend on external services or committed workbook fixtures
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 has durable regression coverage across both service layers
- The repo can prove multi-sheet intake works without changing existing DSL rules
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-sheet-upload-intake/06-03-SUMMARY.md`
</output>
